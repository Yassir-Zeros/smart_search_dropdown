name: smart_search_dropdown
description: A customizable Flutter dropdown with search functionality.
version: 0.0.4
homepage: https://github.com/Yassir-Zeros/smart_search_dropdown.git

example:
  main: example/main.dart

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: